import { UnitConvertPipe } from './unit-convert-pipe.pipe';

describe('UnitConvertPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UnitConvertPipe();
    expect(pipe).toBeTruthy();
  });

  it('should test transform', () => {
    const pipe = new UnitConvertPipe();
  });
});
